version: '3.8'

services:
  chatbot:
    # Override para desarrollo local
    environment:
      - ENVIRONMENT=development
    volumes:
      # Solo logs para debugging en desarrollo
      - ./logs:/app/logs
      # Variables de entorno para desarrollo
      - ./.env:/app/.env:ro
